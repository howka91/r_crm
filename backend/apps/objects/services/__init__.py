"""Business-logic services for the objects app.

Per CLAUDE.md, business logic lives here — not in models, not in signals.
Each module exposes named functions with explicit parameters; callers
(ViewSets, management commands, migrations) invoke them directly.
"""
