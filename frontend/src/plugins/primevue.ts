/**
 * PrimeVue "pass-through" preset — styles every PrimeVue component with Tailwind classes.
 *
 * In Phase 0 this is an empty scaffold; Phase 1 (login) will fill in presets for
 * Button/InputText/Password/Card as they're first used. Keep presets minimal and
 * Tailwind-idiomatic — do not duplicate Prime's default theme.
 */
export const primevuePreset = {
  // Example (to be uncommented when first needed):
  //
  // button: {
  //   root: "inline-flex items-center gap-2 rounded-md px-4 py-2 font-medium " +
  //         "bg-primary-600 text-white hover:bg-primary-700 disabled:opacity-50",
  // },
}
