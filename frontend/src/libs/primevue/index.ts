/**
 * PrimeVue "pass-through" preset — styles every PrimeVue component with
 * Tailwind utilities + the project's ym-* design tokens.
 *
 * Phase 0 leaves this empty; each phase fills in presets for the components
 * it first uses (Button → Phase 1 login, DataTable → Phase 5 clients, ...).
 * Keep presets minimal and Tailwind-idiomatic — do not duplicate Prime's
 * default theme.
 */
export const primevuePreset = {
  // Example (to be uncommented when first needed):
  //
  // button: {
  //   root: "inline-flex items-center gap-2 rounded-md px-4 py-2 font-medium " +
  //         "bg-ym-primary text-white hover:bg-ym-primary-h disabled:opacity-50",
  // },
}
