import type { NavGroup } from "./types"

const references: NavGroup = {
  headerKey: "nav.group_references",
  children: [
    { titleKey: "nav.references_sales_offices", icon: "pi-inbox", disabled: true },
    { titleKey: "nav.references_developers", icon: "pi-wrench", disabled: true },
    { titleKey: "nav.references_contract_templates", icon: "pi-clipboard", disabled: true },
  ],
}

export default references
